import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.keintech.midtermapp',
  appName: 'Mid Term Capa App',
  webDir: 'dist'
};

export default config;
